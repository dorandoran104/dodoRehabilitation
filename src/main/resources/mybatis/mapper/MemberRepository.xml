<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.repository.MemberRepository">
	<insert id="doJoin" parameterType="com.example.demo.dto.MemberDTO">
		insert into member(id,nickname,userid,userpwd,phone,email)
		values(mem_id_seq.nextval,
				#{nickname},
				#{userid},
				#{userpwd},
				#{phone},
				#{email}
		)
	</insert>
	
	<select id="selectUserid" resultType="com.example.demo.dto.MemberDTO">
		select id,
				nickname,
				userid,
				userpwd,
				phone,
				email,
				regdate,
				delmember,
				deldate
		 from member where userid = #{userid}
	</select>
	
	<select id="selectid" resultType="com.example.demo.dto.MemberDTO">
		select 	id,
				nickname,
				userid,
				userpwd,
				phone,
				email,
				regdate,
				delmember,
				deldate
		 from member where id = #{id}
	</select>
</mapper>