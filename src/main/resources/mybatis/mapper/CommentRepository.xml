<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.repository.CommentRepository">
	<select id="getComments" resultType="hashmap">
		select * from commentboard where hpid = #{hpid} order by id desc
	</select>

	<insert id="insertComment" parameterType="com.example.demo.dto.CommentDTO">
		insert into commentboard(id, hpid, body, nickname,userid)
		values(comm_id_seq.nextval,
				#{hpid},
				#{body},
				#{nickname},
				#{userid}
				)
	</insert>
	
	<update id="doModify">
		update commentboard
		set body = #{body},
		updatedate = sysdate
		where id = #{id}
	</update>
	
	<delete id="doDelete">
		delete commentboard
		where id = #{id}
	</delete>
</mapper>